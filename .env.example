# =============================================================================
# API Calendário SPFC - Variáveis de Ambiente
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env no git!
# =============================================================================

# -----------------------------------------------------------------------------
# Firecrawl (obrigatório)
# Obter em: https://www.firecrawl.dev/app/api-keys
# Suporta múltiplas API keys separadas por vírgula para load-balance
# Quando uma key fica sem créditos, automaticamente usa a próxima
# -----------------------------------------------------------------------------
FIRECRAWL_API_KEYS=fc-key1-here,fc-key2-here,fc-key3-here
FIRECRAWL_MAX_RETRIES=3
FIRECRAWL_RETRY_DELAY=5

# -----------------------------------------------------------------------------
# Autenticação da API (obrigatório)
# Gere uma string segura, ex: openssl rand -hex 32
# -----------------------------------------------------------------------------
API_KEY=sua-api-key-segura-aqui

# -----------------------------------------------------------------------------
# Rate Limiting
# Proteção contra abuso da API
# -----------------------------------------------------------------------------
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW=60

# -----------------------------------------------------------------------------
# CORS - Cross-Origin Resource Sharing
# Domínios permitidos a fazer requisições (separados por vírgula)
# Use * para permitir qualquer origem (não recomendado em produção)
# -----------------------------------------------------------------------------
CORS_ORIGINS=https://seudominio.com.br,https://outrodominio.com.br

# -----------------------------------------------------------------------------
# Hosts Permitidos
# Hosts que podem acessar a API (separados por vírgula)
# Use * para permitir qualquer host
# -----------------------------------------------------------------------------
ALLOWED_HOSTS=seudominio.com.br,localhost

# -----------------------------------------------------------------------------
# URL do Calendário SPFC (não precisa alterar)
# -----------------------------------------------------------------------------
SPFC_CALENDARIO_URL=https://www.saopaulofc.net/calendario-de-jogos/
